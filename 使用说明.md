# AI 评论助手 使用说明

## 安装

**方式一：安装包（推荐）**
双击 `AI评论助手 Setup.exe` 安装，从开始菜单或桌面快捷方式启动。

**方式二：绿色版**
解压 `win-unpacked` 整个文件夹到任意位置，双击 `AI评论助手.exe` 运行（exe 必须和文件夹里的其它文件在一起，别单独拷）。

> 桌面版内置了 Node，**不需要另装 Node.js**。首次运行各平台会弹出浏览器，**手动登录一次**即可，之后免登录。

---

## 首次使用

1. 打开程序，左侧「AI 配置」填好 AI 接口（类型/地址/密钥/模型）→ 保存。
2. 第一次对某平台操作时，会弹出浏览器，登录该平台账号（登录态会保存）。

---

## 功能说明

### 1. AI 配置（首页）
配置 AI 接口类型、地址、密钥、模型，保存。支持 OpenAI(`chat`) / Responses / Claude(`messages`) 三种格式。

### 2. 批量导入 📥
粘贴或从 Excel 导入一堆文章链接，自动按平台分类，并回填到各平台页或一键运行页。

### 3. 单平台评论
左侧点「知乎 / 今日头条 / CSDN / 搜狐 / 百度」：
- 填文章链接、每篇评论数 → **开始评论**。
- 支持 **⏸ 暂停 / ▶ 继续**：暂停后从当前进度继续，不会从头来。**停止** = 终止，下次从头。

### 4. 一键运行 🚀
- 一次填入各平台链接 + 评论数 → **🚀 开始全部**。
- 串行评论全部平台 → 评论完自动进入检测阶段 → 结果汇总到「检测中心」。
- 同样支持 暂停 / 继续。

### 5. 检测中心 🔍（独立检测 + 达标管理）

**基本检测**
- 粘贴文章链接（自动识别平台）→ **🔍 一键检测**。
- 结果表显示：平台 / 文章链接(可点击，用系统浏览器打开) / 我的评论 / 点赞 / 收藏 / 达标状态。
- 每行可点「重新检测」单独重测；可「导出 CSV」。

**达标判定**（硬性要求，写死）
- 达标 = **评论 ≥ 3 条** 且 **已点赞** 且 **已收藏**。
- 状态：🟢达标 / 🔴不达标(标出缺啥，如"缺2评论·缺赞") / ⚪待核对(评论数未知) / ⚠已失效(404)。

**自动补跑到达标（半自动）**
- **🛠 补跑不达标项**：自动给不达标文章再跑一遍 评论+点赞+收藏（评论按缺口补到 3）。
- **🔄 重测未达标项**：补跑后重新检测这些文章，看哪些变达标。
- 可反复「补跑 → 重测」直到全部达标；**每篇最多补跑 2 次**，到上限标"已达上限·人工"，不再死磕。
- 404/被删文章标「⚠ 已失效」，自动排除补跑与重测。

**检测账号设置 👤**
- 检测中心点「👤 检测账号」弹窗，填各平台账号名。
- **支持多个账号，用逗号分隔**（半角 `,` / 全角 `，`），命中任意一个就算我的评论。
- 换账号时改这里 → 保存 → 重新检测。手填优先于页面自动识别。

---

## 运行日志
- 桌面版：`%APPDATA%\AI评论助手\logs\run_年-月-日_时-分-秒.log`
- 绿色版(bat)：程序目录下 `logs\`
- 日志不会自动清理，不需要时可手动删整个 `logs` 目录。

## 常见问题

**Q：检测显示"待核对" / 评论数为未知？**
A：没识别到账号名。去「👤 检测账号」手动填上该平台的账号名，再重新检测。

**Q：某平台检测不出来？**
A：先确保该平台在程序浏览器里登录过（跑一次评论任务会触发登录）。登录后再检测。

**Q：点文章链接打不开？**
A：链接用系统默认浏览器打开，需在系统浏览器里登录该平台账号。

**Q：评论失败 / AI 生成失败？**
A：检查 AI 配置是否正确、是否已登录平台、查看日志区错误提示。

**Q：暂停后没立刻停？**
A：暂停在「文章之间」生效：当前这篇文章会跑完，下一篇前停住。要立刻打断用「停止」。

---

## 平台支持
| 平台 | 自动评论 | 评论数检测 | 点赞/收藏检测 |
|------|:--:|:--:|:--:|
| 知乎 / 今日头条 / CSDN | ✅ | ✅ | ✅ |
| 百度百家号 | ✅ | ✅ | ✅ |
| 搜狐 | ✅ | ❌ 暂不支持 | ❌ |

> 建议：每篇文章评论数 1~3 条，适度间隔，避免触发平台风控。
